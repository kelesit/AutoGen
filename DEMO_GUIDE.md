# 给合作方的业务流程与可靠性演示

启动 `npm run dev`，打开 http://127.0.0.1:5173 。先用管理员演示账户发布模板，随后用普通账户走创作流程；管理员账户还能在任务详情里重放事件。演示不连接真实模型或支付；生成输出都是同一段固定样片。

## 先演示动作模板业务闭环

使用管理员账户进入“管理后台 → 模板目录”，填写模板名称和分类，选择单人或双人图片槽位，填写与参考画面对应的角色说明，上传 1–3 段动作参考视频和一段 MP4 示例成片，配置允许的规格后发布。切换到 Explore，应立即看到新模板；示例成片自动播放。参考视频只进入模板版本，不作为广场预览。

用普通演示账户进入 Explore，打开刚发布的模板。按图片槽位上传素材，选择允许的规格并获取报价，提交后在 Collection 看任务；任务详情保存模板版本、动作视频 ID、图片槽位映射和报价。模板版本过期或参数超出允许范围时，服务端拒绝提交。

Train 页面已移除，本轮仅演示管理员发布模板。旧版兼容接口和静态模板已移除，可靠性测试使用真实目录模板。当前生成仍返回固定演示样片，这段演示只验证业务流程。

## 1. 一条完整业务链路

选择模板，按槽位上传人物图，需要时补充描述；切换规格观察服务端报价。提交后进入作品库，在“积分记录”中展示可用与冻结两个余额。

打开“查看任务详情与恢复记录”，展示外部任务 ID、模板和报价快照。供应商生成成功后还有“保存作品”阶段；文件保存成功才结算。播放样片，并展示私有文件的大小和摘要。

讲解：“用户积分与供应商成本单独核算。结果能交付，才按这个演示的规则结算用户积分。”

所有故障场景均在当前动作模板生成窗口的“演示选项”内，默认折叠、默认正常生成。先完成必填图片上传，再选择故障场景或丢弃响应开关。

## 2. 供应商已接单，但响应丢失

打开动作模板 → 上传人物图片 → 展开“演示选项” → 选择“已接单，但响应丢失”。

任务详情最终应显示：提交异常 → 接单结果未知 → 查单恢复原供应商 ID → 保存 → 一次结算。外部调用列表只有一次提交。

讲解：“这里如果直接自动重试生成，可能重复付给供应商；所以我先查原任务。”

## 3. 生成成功，首次保存失败

新建任务，选择对应故障场景。详情中应有一次提交、两次保存调用，最终一份作品、一次结算。等待过程中可查看冻结余额。

讲解：“生成和保存是两个阶段，下载失败不会触发第二次生成。”

## 4. 重复和乱序事件

在已完成任务详情中，以管理员点击“演示重复 / 乱序事件”。时间线增加管理员操作和 4 条忽略记录；完成状态不回退，账务保持原结算。

说明这是内部供应商事件演示；本版本没有伪装成真实 Webhook。

## 5. 浏览器丢失提交响应，刷新后恢复

创建新任务，勾选“模拟浏览器丢弃提交响应”。提交后出现明确的演示错误。刷新页面，顶部仍有“有一笔提交结果等待确认”。点击“查询并恢复”，应找回原任务，作品数量只增加一笔。

讲解：“请求键在发送前就保存了，刷新页面不会因为换了请求键而重复创建和冻结。”

## 6. 诚实处理无法自动恢复的情况

最后再演示“响应丢失，且供应商不支持查单”。任务进入“需要人工核查”，积分继续冻结。后台恢复按钮会解释为什么不能安全重做。

此任务有意不能自动结束，也会保守占用一个生成名额。不要连续创建多笔该场景用于日常展示；它用于证明明确的能力边界。恢复真实业务需供应商提供最终状态证据，本演示没有无证据的强制放款按钮。

## 7. 用测试证明进程退出后的恢复

运行 `npm test`。其中两项测试在独立子进程中分别于“供应商接单后”和“文件落盘后”立即退出，再由另一执行器恢复，验证没有新生成任务或重复结算。

可以配合 [ARCHITECTURE.md](./ARCHITECTURE.md) 说明：数据库执行记录、短事务、租约、外部接单不确定性、文件与账务边界。

## 模板纠错和草稿恢复

后台填写部分发布信息，切换任务列表再返回，内容应保留；刷新后文本与完整的已上传素材引用恢复。尚未上传的本地文件需要重新选择。

模板目录默认显示管理列表，可按名称、说明或 ID 搜索，按状态和分类筛选，并打开详情。点击“编辑”可修改元信息、动作参考视频、Prompt、图片槽位、输出规格、示例成片及上下架状态。生成配置变更保存新版本，历史任务快照保持原版本。

删除时显示引用任务数量并二次确认。删除后的模板退出广场且不能新建任务；历史作品仍可播放下载。删除不可恢复；“已删除”筛选查看删除时保留的当前配置，不提供历史版本浏览。该详情页显示删除状态提示，不再渲染媒体播放器；历史配置存在不代表历史文件仍可播放。

## 额外场景

- “首次提交被限流”：全局暂停新提交并退避；后台可看到暂停状态。
- “供应商确认生成失败”：用户冻结积分释放，供应商模拟成本仍保留。
- 排队时可取消；进入供应商提交后不能假装取消成功并退款。
- 管理后台点击任务名称进入详情；普通用户只能查看自己的任务详情。

## 演示边界

真实模型/支付未接入，供应商为独立数据库的本地适配器模拟，不是网络服务。底层是真实事务、文件保存、进程恢复和权限校验。不能把这些测试说成已完成真实供应商联调或生产容量验证。


## 作品与素材删除

在“我的作品”中对已完成作品点击“删除作品”，确认后不可播放、下载或恢复；任务和积分记录保留，不退款。只有尚未提交的排队任务可以取消。

管理后台“素材库”展示当前管理员上传的动作参考与示例视频。当前模板或未结束任务使用中的素材不能删除；先替换/删除模板，等待关联任务结束后，再删除不需要的素材。后台 Worker 清理文件；无需手工操作磁盘。

新开发数据在 `.data-v2`，原 `.data` 不再读取。启动必须同时运行 API 和 Worker（推荐 `npm run dev`）。无 Worker 时请求仍会持久排队，后台显示 Worker 离线。
